<div class="row">
    <div class="col-md-12">
        <h1 class="page-title">{{file.name}}</h1>
        {{#each tags}}
        <div class="col-sm-3 wq-mb-10">
            <a class="btn btn-primary btn-block" href="/tags/{{name}}"><span class="fui-tag fl"></span>{{name}}</a>
        </div>
        {{/each}}
    </div>
</div>

